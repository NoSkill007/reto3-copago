# Triage labels

Cada rol corresponde a una etiqueta del mismo nombre:

- `needs-triage`: pendiente de evaluación.
- `needs-info`: requiere información del autor.
- `ready-for-agent`: especificado y listo para un agente.
- `ready-for-human`: requiere implementación humana.
- `wontfix`: no se implementará.
